# generated (output/ PDFs ARE committed in your private copy so you can download them from the GitHub app;
# add `output/*.pdf` here if you prefer to keep them local)
__pycache__/
*.pyc
.DS_Store

# interviews: audio and local state never leave your machine
interviews/inbox/*
!interviews/inbox/.gitkeep
interviews/.processed.json
*.m4a
*.mp3
*.wav
*.aiff
*.caf
*.ogg
*.flac

# working files the agent may create
cv.json
/tmp/
